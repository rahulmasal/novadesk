/**
 * Setup Page - Renders the Setup Wizard for first-time configuration
 */
import { SetupWizard } from "@/components/SetupWizard";

export default function SetupPage() {
  return <SetupWizard />;
}